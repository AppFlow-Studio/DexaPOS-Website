---
phase: outside-gsd (direct feature work on /manage/transactions)
status: in_progress
last_updated: 2026-02-15
---

<current_state>
Working on the HQ Admin Transactions page (`/manage/transactions`).
Two tickets completed: ADM-006 (Advanced Filter Panel) + ADM-007 (Global Search).
User is currently TESTING the filters. Also just added `schema.sql` to workspace root and wants it explained.
</current_state>

<completed_work>

1. Fixed basic filter/export bugs on the original page:
   - Wired up search input (was decorative)
   - Export CSV button now downloads a file
   - Fixed `business_name` bug (merchants table only has `name` column)
   - Fixed wrong order status values (`voided` → `void`, added `draft`, `preparing`, `ready`)

2. Rewrote server action (`app/manage/actions/hq-platform/transactions.ts`):
   - Changed primary table from `orders` → `order_payments` (join orders!inner → merchants)
   - Added full filter support: merchantIds, locationIds, orderStatuses, paymentStatuses, paymentMethods, cardTypes, minAmount, maxAmount, dateFrom, dateTo, search
   - Fixed PostgREST filter syntax: `.in('orders.column')` does NOT work on related tables — must use `.filter('orders.column', 'in', '(val1,val2)')`
   - Added `getPlatformMerchants()` and `getPlatformLocations()` helper actions

3. Created `app/manage/transactions/components/TransactionFilterSheet.tsx`:
   - Slide-out Sheet with all 8 filter sections
   - Multi-select dropdowns (DropdownMenuCheckboxItem pattern)
   - DateRangePicker reused from `components/dashboard/orders/DateRangePicker.tsx`
   - URL sync via `router.push` with URLSearchParams
   - Active filter count badge on trigger button
   - Re-syncs local state from URL when sheet opens (so reopening shows current filters)

4. Created `app/manage/transactions/components/TransactionSearchBar.tsx`:
   - Debounced 300ms via `lib/hooks/useDebounce.ts`
   - Min 2 chars before triggering
   - × clear button
   - Cmd+K / Ctrl+K focus shortcut
   - `highlightText()` helper exported for table cell highlighting

5. Rewrote `app/manage/transactions/page.tsx`:
   - useSearchParams for URL-driven filter state (shareable URLs)
   - Wrapped in `<Suspense>` (required by Next.js for useSearchParams)
   - Table now shows: Order #, Merchant + Location, Customer, Method, Card, Total, Pay Status, Order Status, Date
   - Export CSV includes all new fields

6. Updated `lib/queries/use-platform-analytics.ts`:
   - `usePlatformTransactions` now accepts `filters?: PlatformTransactionFilters`

</completed_work>

<remaining_work>

- User is testing filters now — may find more bugs
- User added `schema.sql` (2349 lines) to workspace root — wants it explained
- Potentially more tickets from the same spec to implement after testing passes

</remaining_work>

<decisions_made>

- Switched primary table from `orders` to `order_payments` so payment data (card type, auth code, etc.) is natively available without sub-queries
- Used `.filter()` not `.in()` for related table column filtering in PostgREST
- Filter sheet uses local state + "Apply Filters" button (not live filtering) — matches Sheet UX pattern
- URL params as source of truth for filter state (shareable, back-button safe)
- `referencedTable: 'orders'` option for `.or()` search across joined table columns

</decisions_made>

<blockers>

- PostgREST filtering on related table columns is tricky — fixed with `.filter()` syntax
- The search `.or()` with `{ referencedTable: 'orders' }` option is untested — may need adjustment
- `merchant_locations` table name used in `getPlatformLocations()` — verify this is correct vs `locations`

</blockers>

<context>
The admin transactions page is being built to match the quality of `app/dashboard/payments/` page.
The spec has 7 tickets (ADM-001 through ADM-007). We completed ADM-006 and ADM-007.
The other tickets (ADM-001 through ADM-005) would cover things like transaction detail drawer, stats cards with real aggregates, charts, etc.

Key files:
- `app/manage/transactions/page.tsx` — main page
- `app/manage/transactions/components/TransactionFilterSheet.tsx` — filter panel
- `app/manage/transactions/components/TransactionSearchBar.tsx` — search bar
- `app/manage/actions/hq-platform/transactions.ts` — server action (all data fetching + filtering)
- `lib/queries/use-platform-analytics.ts` — TanStack Query hook
- `schema.sql` — full DB schema added by user to workspace root (2349 lines, not yet read)
</context>

<next_action>
1. Read `schema.sql` and explain the full database schema to the user
2. After user confirms filters are working, look at remaining ADM tickets or whatever they bring next
</next_action>
