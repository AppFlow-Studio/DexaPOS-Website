---
phase: outside-gsd (direct feature work on /manage/transactions)
status: in_progress
last_updated: 2026-02-15
---

<current_state>
ADM-001 is complete: transaction detail drawer is live from row actions.
ADM-004 is complete: refund is now wired from row menu with confirmation and backend execution.
ADM-006 is complete and stable.
ADM-007 is complete: search works across customer/order/payment/merchant/location fields using `vw_platform_transactions`.
Fallback logic exists so transactions still load via legacy query if the view is not available in an environment.
</current_state>

<completed_work>

1. Transactions data/search backend (`app/manage/actions/hq-platform/transactions.ts`):
   - Added view-based query path (`vw_platform_transactions`) for full search coverage.
   - Added shared filter application for merchant/location/status/method/card/date/amount.
   - Added safe fallback to legacy `order_payments -> orders -> merchants -> locations` query when view is missing.

2. DB migration:
   - Added `supabase/migrations/021_platform_transactions_view.sql`.
   - User applied migration in DB and confirmed search is now working.

3. Export behavior (`app/manage/transactions/page.tsx`):
   - Export now retrieves all rows matching current filters in batches (not only current table page).

4. Filter sheet UX polish (`app/manage/transactions/components/TransactionFilterSheet.tsx`):
   - Fixed content spacing/padding and structured sheet layout.
   - Fixed overlap between sheet close X and "Clear all".
   - Fixed dropdown z-index class (`z-[200]`).

5. Refund data hygiene:
   - Removed incorrect `refunded_amount: 0` placeholder write from admin refund update logic (`app/manage/actions/admin-merchant/transactions.ts`).

6. ADM-001 transaction detail drawer:
   - Added server action `getPlatformTransactionDetails(transactionId)` to fetch detailed payment + order + line-item context.
   - Added query hook `usePlatformTransactionDetails`.
   - Added UI component `app/manage/transactions/components/TransactionDetailSheet.tsx`.
   - Wired `View Details` action in the transactions row menu to open the drawer.

7. ADM-004 refund action from row menu:
   - Added server action `refundPlatformTransaction(transactionId, reason)`.
   - Added confirmation dialog + action flow in `app/manage/transactions/page.tsx`.
   - Refund button is enabled only for `captured` statuses.
   - On success, invalidates platform transactions and KPI query caches.

8. Card type filter + card brand UI polish:
   - Updated card type filter logic to handle common DB card-type variants and single-card-type partial matching.
   - Updated matching to OR across known brand tokens.
   - Status: `Visa` filter works; `Mastercard`/other types still inconsistent and deferred for exact DB-value mapping.

9. Manage-table parity with dashboard payments:
   - Reused dashboard `CardBrandIcon` for card-brand rendering in manage transactions table and detail drawer.
   - Added sortable headers for `Order #` and `Date` in manage transactions table.
   - Added manual `Refresh` button to re-fetch transaction and KPI queries.

</completed_work>

<remaining_work>

- ADM-002: Stats cards backed by real DB aggregates
- ADM-003: Revenue/volume charts

</remaining_work>

<decisions_made>

- Keep view-based search as primary path for full-column search support.
- Keep legacy fallback path to prevent runtime outages when DB view is missing in any env.
- Keep URL params as source of truth for filter and search state.
- Export should respect filters globally, not just visible page rows.
- Implement transaction details from `order_payments` joined to `orders` so drawer works regardless of view availability.
- Reuse existing `refundAdminOrder` path for platform refunds to keep business logic and audit behavior consistent.
- Prefer visual card-brand marks over raw card-type text in transaction UI.

</decisions_made>

<blockers>

Card-type filtering for some non-Visa brands is still inconsistent in production-like data.
Deferred follow-up: inspect distinct `card_type` values from DB and map filters exactly.
(Need to ensure migration `021_platform_transactions_view.sql` is applied in every environment.)

</blockers>

<context>
Ticket scope: ADM-001 through ADM-007 for `/manage/transactions`.
Primary files touched:
- `app/manage/actions/hq-platform/transactions.ts`
- `lib/queries/use-platform-analytics.ts`
- `app/manage/transactions/page.tsx`
- `app/manage/transactions/components/TransactionDetailSheet.tsx`
- `app/manage/transactions/components/TransactionFilterSheet.tsx`
- `supabase/migrations/021_platform_transactions_view.sql`
- `app/manage/actions/admin-merchant/transactions.ts`
</context>

<next_action>
1. Build ADM-002 real aggregate stats cards.
2. Then ADM-003 revenue/volume charts.
</next_action>


