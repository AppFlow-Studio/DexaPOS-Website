---
phase: outside-gsd (official ADM-001..019 alignment)
status: in_progress
last_updated: 2026-02-17
---

<current_state>
Tracker now covers official ADM-001 through ADM-019.
ADM-001, ADM-003, ADM-006, ADM-007, ADM-008, ADM-009, ADM-010 are done.
ADM-011 and ADM-012 are implemented and pending final QA scratch-off.
ADM-002, ADM-004, ADM-005 are implemented and pending migration apply + QA.
ADM-015 is QA awaiting (export implementation complete, pending QA signoff).
ADM-013 implementation is complete in code and pending migration apply + QA.
ADM-016 implementation is complete in code and pending migration apply + QA.
ADM-014 implementation is complete in code and pending migration approval + QA.
ADM-019 implementation is complete in code and pending migration approval + QA.
ADM-017 is implemented and pending QA. ADM-018 is implemented and pending ADM-019 migration approval + QA.
Experimental transactions trend graph in `/manage/transactions` is done and awaiting QA.
</current_state>

<ticket_status>
- ADM-001: Done (user-confirmed)
- ADM-002: Implemented (pending migration apply + QA)
- ADM-003: Done (user-confirmed)
- ADM-004: Implemented (pending migration apply + QA)
- ADM-005: Implemented (pending migration apply + QA)
- ADM-006: Done
- ADM-007: Done
- ADM-008: Done (user-confirmed)
- ADM-009: Done
- ADM-010: Done (user-confirmed)
- ADM-011: Done (ready for QA)
- ADM-012: Done (ready for QA)
- ADM-013: Implemented (pending migration apply + QA)
- ADM-014: Implemented (pending migration approval + QA)
- ADM-015: QA awaiting
- ADM-016: Implemented (pending migration apply + QA)
- ADM-017: Implemented (pending QA)
- ADM-018: Implemented (pending ADM-019 migration approval + QA)
- ADM-019: Implemented (pending migration approval + QA)
</ticket_status>

<completed_work>

1. Transactions platform implementation (ADM-006 through ADM-012 scope delivered):
- Advanced filters, URL-synced search/filters/sort/page/pageSize, pagination, column controls, sticky header, zebra rows.
- Expanded transaction detail with 2x2 details, split segments, adjustments/reversals, timeline, and full order breakdown.
- Refresh/export basics and card-brand visual parity.

2. Backend foundation (ADM-002, ADM-004, ADM-005 implemented):
- Added RLS/merchant-scope migration and helper function.
- Added `get_admin_transactions(...)` and `get_admin_transaction_detail(...)` RPC migrations.
- Wired app actions to RPC-first with compatibility fallback to existing view/legacy paths.

3. Tracking and documentation:
- Added migration + QA runbook: `ADM-002-004-005-APPLY-QA.md`.
- Updated task status tracker to include ADM-001..019.
- Documented transaction volume drop (`1119 -> 200`) as expected scoping/filter behavior.
- Added `ADM-RLS-AUDIT-CHECKLIST.md` for pre/post RLS validation (merchant + HQ policy safety checks).

4. UX bug fix completed today:
- `Apply Filters` now closes the filter sheet even when no URL-param delta exists.

5. ADM-015 implementation completed in code:
- Added migration `supabase/migrations/025_adm_015_admin_transactions_export_rpc.sql` with `get_admin_transactions_export(...)`.
- Added server action `getPlatformTransactionsExport(filters)` with RPC call, 10,000 cap handling, and overflow detection.
- Upgraded transactions header export to dropdown (`CSV` + `Excel (.xlsx)`), all required columns export mapping, loading state, and cap warning message.
- Added filename format: `DEXA_Transactions_{merchant}_{date_from}_to_{date_to}.{ext}`.
- Added export dependencies: `papaparse`, `xlsx`, `@types/papaparse`.
- Added ADM-015 apply/QA runbook: `ADM-015-APPLY-QA.md`.

6. Experimental transactions graph completed:
- Added experimental "Transactions Trend" area chart on `/manage/transactions`.
- Uses live `usePlatformSalesTrend()` data with loading and empty fallbacks.
- Marked done pending QA verification.

7. ADM-016 implementation completed in code:
- Added migration `supabase/migrations/027_adm_016_batch_reconciliation_rpc.sql`.
- Added RPCs:
  - `get_admin_settlement_batches(...)`
  - `get_admin_settlement_batch_payments(...)`
- Added server actions and hooks for batch list + selected batch payments.
- Added `Batch Reconciliation` section in `/manage/transactions` with:
  - merchant/date/status filters,
  - discrepancy detection,
  - row click drill-down to linked payments,
  - CSV export for selected batch.
- Added runbook: `ADM-016-APPLY-QA.md`.

8. ADM-013 implementation completed in code:
- Added migration `supabase/migrations/026_adm_013_admin_transaction_summary_rpc.sql` with `get_admin_transaction_summary(...)`.
- Added server action `getPlatformTransactionSummary(filters)` and query hook `usePlatformTransactionSummary(filters)`.
- Replaced top transaction summary block with 6 dynamic cards (Total Transactions, Card Revenue, Cash Revenue, Total Revenue, Avg Tip, Voided/Returned).
- Added prior-equivalent-period delta behavior and no-date default to rolling last 30 days.
- Added card-click filter actions with URL-param updates and page reset behavior.
- Added summary fallback text when migration 026 is missing.
- Added ADM-013 apply/QA runbook: `ADM-013-APPLY-QA.md`.

9. ADM-014 implementation completed in code:
- Added migration `supabase/migrations/028_adm_014_merchant_breakdown_rpc.sql`.
- Added RPC:
  - `get_admin_merchant_breakdown(...)`
- Added server action and hook:
  - `getPlatformMerchantBreakdown(filters)`
  - `usePlatformMerchantBreakdown(filters)`
- Added `Merchant Breakdown` collapsible section in `/manage/transactions`:
  - hidden by default,
  - sortable by all required columns,
  - includes merchant-level sparkline trend,
  - responds to active date range and merchant/location/payment-status filters.
- Added runbook: `ADM-014-APPLY-QA.md`.
- Migration approval note:
  - Current approved baseline to apply: `supabase/migrations/028_adm_014_merchant_breakdown_rpc.sql`.
  - Team-shared v2 SQL is pending approval before apply in shared/prod-like environments.
  - Why pending: it changes metric/output semantics versus current UI/types, narrows card method logic, and can undercount legacy rows without fallback for null denormalized merchant/location fields.

10. ADM-019 implementation completed in code:
- Added migration `supabase/migrations/029_adm_019_admin_payment_audit_logging.sql`.
- Added RPC:
  - `log_admin_payment_audit_event(...)`
- Added non-blocking audit logging hooks in `app/manage/actions/hq-platform/transactions.ts` for:
  - transaction list views,
  - transaction detail expansion (`view_payment_detail`),
  - export events (`export_data`),
  - card-last-four searches (`search_card_last_four`).
- Added `fields_accessed` mapping:
  - list/export -> `['card_last_four', 'auth_code']`
  - detail -> `['card_last_four', 'auth_code', 'emv_data']`
- Added runbook: `ADM-019-APPLY-QA.md`.

11. ADM-018 implementation completed in code:
- Added section component: `app/manage/transactions/components/AuditLogSection.tsx`.
- Added server action:
  - `getPlatformPaymentAuditLogs(filters, limit, offset)`
- Added query hook:
  - `usePlatformPaymentAuditLogs(filters, limit, offset)`
- Added UI section in `/manage/transactions`:
  - collapsible payment audit log viewer,
  - filters (user/action/merchant/outcome/date),
  - search by user email/resource id,
  - failed-action highlight,
  - required audit columns and `fields_accessed`.
- Dependency note:
  - data depends on ADM-019 migration `029_adm_019_admin_payment_audit_logging.sql` approval/apply.

12. ADM-017 implementation completed in code:
- Added section component: `app/manage/transactions/components/ChargebacksSection.tsx`.
- Added server action:
  - `getPlatformChargebacks(filters, limit, offset)`
  - returns chargeback rows plus `pendingCount` and `urgentCount`.
- Added query hook:
  - `usePlatformChargebacks(filters, limit, offset)`
- Added UI section in `/manage/transactions`:
  - filters by merchant/status/card network/date,
  - urgency alert for deadlines within 72 hours,
  - pending/notified badge,
  - expandable row with original payment context, defense documents, and resolution details.

</completed_work>

<adm_013_decision_log>

These choices were explicitly confirmed with the user and locked during ADM-013 planning:

1) Prior-period comparison base
- Immediate previous equivalent window (not YoY).
- Example: Jan 10-16 compares to Jan 3-9.

2) No date selected fallback
- Rolling last 30 days vs previous 30 days.

3) Card click behavior
- Toggle related filter and preserve all other active filters/search.

4) Revenue rule
- Captured-only for Card Revenue, Cash Revenue, and Total Revenue.

5) Avg tip percent formula
- Average of `tip_amount / amount` for captured card-family payments (ignore zero/NULL amount).

6) Voided/Returned formula
- Combined unique payments where `is_voided OR is_returned`.
- Amount precedence: `return_amount` when returned, else `total_amount` when voided.

7) Total Transactions card click action
- Clear/reset payment method filter to all, preserve all other filters.

8) ADM-015 status decision
- Updated to `QA awaiting`.

</adm_013_decision_log>

<data_volume_note>

- Date recorded: 2026-02-17
- Observed: transactions shown dropped from `1119` to `200`.
- Root cause:
  1) ADM-002 merchant scoping via `get_admin_merchant_ids()`.
  2) ADM-004 default excludes `pending` and `failed` when payment status filter is unset.
- Impact: expected scoped/filtered result, not deleted records.

</data_volume_note>

<remaining_work>

- Apply and validate migrations `022`, `023`, `024`, `026`, and `027` in target environments.
- ADM-014 migration is pending approval decision:
  - baseline file ready: `028_adm_014_merchant_breakdown_rpc.sql`
  - team v2 SQL requires alignment/review before apply
- Finish QA and scratch ADM-011 and ADM-012.
- Finish QA and scratch ADM-013, ADM-014, ADM-015, ADM-016, and ADM-019 (after migration approval + apply).
- Run ADM-018 QA after migration `029` approval/apply.
- Run ADM-017 QA and scratch after verification.
- Use `ADM-RLS-AUDIT-CHECKLIST.md` before any future RLS policy migration.

</remaining_work>

<plan_013_019>

1. ADM-013: enhanced summary cards with live aggregates
- Backend: `get_admin_transaction_summary(...)` RPC.
- Frontend: filter-reactive cards, prior-period delta subtext, clickable card-to-filter actions.

2. ADM-014: merchant comparison breakdown
- Implemented in code; pending migration approval + QA.
- Apply target (if approved): `supabase/migrations/028_adm_014_merchant_breakdown_rpc.sql`.

3. ADM-018:
- Implemented in code; pending ADM-019 migration approval + QA.
- Next for ADM-018 is QA pass against live `payment_audit_log` data.

4. ADM-017:
- Implemented in code; pending QA verification.
- Next for ADM-017 is validation of filter behavior, urgency banner, and expanded dispute detail payload.

</plan_013_019>

<blockers>

No hard blockers.
Known repo-wide TypeScript errors exist outside the transactions scope and are unrelated to this work.

</blockers>

<context>
Primary status source for user review:
- `ADM-006-007-STATUS.md` (now full ADM-001..019 task tracker)

Primary implementation files:
- `app/manage/actions/hq-platform/transactions.ts`
- `app/manage/transactions/page.tsx`
- `app/manage/transactions/components/BatchReconciliationSection.tsx`
- `app/manage/transactions/components/MerchantBreakdownSection.tsx`
- `app/manage/transactions/components/ChargebacksSection.tsx`
- `app/manage/transactions/components/AuditLogSection.tsx`
- `app/manage/transactions/components/TransactionFilterSheet.tsx`
- `app/manage/transactions/components/TransactionDetailInlinePanel.tsx`
- `lib/queries/use-platform-analytics.ts`
- `supabase/migrations/027_adm_016_batch_reconciliation_rpc.sql`
- `supabase/migrations/028_adm_014_merchant_breakdown_rpc.sql`
- `supabase/migrations/029_adm_019_admin_payment_audit_logging.sql`

</context>

<next_action>
1. Confirm ADM-014 SQL version approval (`028` baseline vs team v2), then apply approved script and run ADM-014 QA.
2. Apply migration `027_adm_016_batch_reconciliation_rpc.sql` and run ADM-016 QA.
3. Run QA pass for ADM-015 export and experimental transactions trend graph.
4. Apply migration `026_adm_013_admin_transaction_summary_rpc.sql`.
5. Run focused QA for ADM-013 metrics, card click filters, and prior-period comparisons.
6. Confirm approval for migration `029_adm_019_admin_payment_audit_logging.sql`, then apply and run ADM-019 QA.
7. Run ADM-018 QA after migration `029` is approved/applied, then run ADM-017 QA and scratch if passed.
8. For any RLS changes, run `ADM-RLS-AUDIT-CHECKLIST.md` pre/post and share policy diff before prod.
</next_action>
