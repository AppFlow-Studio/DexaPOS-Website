---
phase: outside-gsd (official ADM-001..012 alignment)
status: in_progress
last_updated: 2026-02-17
---

<current_state>
Tracker remains aligned to the official ADM-001..012 ticket definitions from the user.
ADM-010 implementation is user-confirmed done.
ADM-011 and ADM-012 implementation is complete in code and marked ready for QA scratch-off.
ADM-002/004/005 backend work is now implemented in migrations and wired in app actions, pending migration apply + QA verification.
</current_state>

<ticket_status>
- ADM-001: Done (user-confirmed)
- ADM-002: Implemented (pending migration apply + QA)
- ADM-003: Done (user-confirmed)
- ADM-004: Implemented (pending migration apply + QA)
- ADM-005: Implemented (pending migration apply + QA)
- ADM-006: Done
- ADM-007: Done
- ADM-008: Done (user-confirmed)
- ADM-009: Done
- ADM-010: Done (user-confirmed)
- ADM-011: Done (ready for QA)
- ADM-012: Done (ready for QA)
</ticket_status>

<completed_work>

1. Implemented production UI/data improvements across `/manage/transactions`:
- search, advanced filters, export-all, refund action, detail drawer, card brand icons, refresh, basic sorting.
- completed remaining ADM-008 UI scope requested by user: new table columns (Customer fallback, Entry, Subtotal, Tax, Tip, Discount, Staff), column visibility toggle defaults, sticky header, alternating row shading.
- completed ADM-006 remaining points: searchable staff filter, debounced (300ms) filter URL sync, and verified shareable URL filter persistence.
- completed ADM-009: server-side table sorting (Order/Total/Date), URL-persisted sort/page/pageSize, page-size selector (25/50/100), page number input, prev/next controls, showing-range label, and loading skeleton while refetching.
- completed ADM-010 implementation scope: inline row expansion (single open row), 2x2 detail grid, split-payment segment cards, copy actions, loading state, and adjustment/reversal detail blocks.
- completed ADM-011 implementation scope: payment timeline section with event icons, status transitions, relative/absolute timestamps, and collapsible raw JSON.
- completed ADM-012 implementation scope: full order breakdown with all order items, modifier indentation, void/open-item/tax-exempt indicators, order-level discounts, and totals row.
- hardened detail mapping for boolean flag coercion from mixed DB types and normalized discount sign display.
- implemented ADM-002 migration with `get_admin_merchant_ids()` and additive HQ SELECT RLS policies for:
  - `orders`
  - `order_payments`
  - `order_items`
  - `order_payment_items`
  - `payment_events`
- implemented ADM-004 migration with formal `get_admin_transactions(...)` RPC and server-action RPC-first wiring with fallback.
- implemented ADM-005 migration with formal `get_admin_transaction_detail(p_order_id)` RPC and detail RPC-first wiring with fallback.
- documented data-volume change note (`1119 -> 200`) with root cause:
  - merchant assignment scoping (ADM-002),
  - default exclusion of `pending`/`failed` when payment status filter is empty (ADM-004).

2. Added/updated server actions:
- list transactions, detail fetch, refund trigger, filtered aggregate stats.

3. Added tracking alignment:
- rewrote `ADM-006-007-STATUS.md` to official 12-ticket comparison format.

</completed_work>

<data_volume_note>

- Date recorded: 2026-02-17
- Observed: transactions shown dropped from `1119` to `200`.
- Root cause:
  1) ADM-002 admin scope is active, so data is limited to merchants returned by `get_admin_merchant_ids()`.
  2) ADM-004 default behavior excludes `pending` and `failed` payment statuses when no payment-status filter is selected.
- Impact: this is expected filtering behavior, not deleted records.

</data_volume_note>

<remaining_work>

- Main unresolved backend work: apply and validate migrations `022`, `023`, `024` in target DB environments.
- Main unresolved frontend scope: QA verification for ADM-011 and ADM-012 on representative data.

</remaining_work>

<decisions_made>

- Use strict ticket acceptance-criteria matching before marking a ticket "Done".
- Keep user-confirmed crossed-out tickets (ADM-001, ADM-003) as done without overriding.
- Treat implemented behavior that differs from spec shape as Partial/Not started by strict spec parity.

</decisions_made>

<blockers>

No hard technical blockers. Remaining risk is data-shape variance by environment (e.g., optional tables like payment_events/order_discounts), already handled with compatibility fallbacks.

</blockers>

<context>
Primary status source for user review:
- `ADM-006-007-STATUS.md` (now remapped to ADM-001..012)

Execution context files:
- `.planning/.continue-here.md`
- `app/manage/actions/hq-platform/transactions.ts`
- `lib/queries/use-platform-analytics.ts`
- `app/manage/transactions/page.tsx`
- `app/manage/transactions/components/TransactionDetailInlinePanel.tsx`

</context>

<next_action>
1. Apply migrations `022_adm_002_admin_rls.sql`, `023_adm_004_admin_transactions_rpc.sql`, `024_adm_005_admin_transaction_detail_rpc.sql`.
2. Run QA for ADM-002/004/005 with assigned-admin and super-admin accounts.
3. Run QA pass for ADM-011 and ADM-012 and scratch them if accepted.
</next_action>
